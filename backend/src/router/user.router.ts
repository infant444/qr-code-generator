// import { Model,Schema } from "mongoose";

import asynceHandler from 'express-async-handler';
import  Router  from 'express';

const router=Router()

router.get('/signin',asynceHandler(
    async(req,res)=>{
        
    }
))
export default Router;


